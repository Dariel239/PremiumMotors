@* _ViewStart.cshtml runs before every view in this folder tree. By setting Layout once here,
   every page is automatically wrapped in Views/Shared/_Layout.cshtml (the common header,
   nav and footer) without each view having to declare it. A view can still override this by
   setting its own Layout (or Layout = null for a standalone page). *@
@{
    Layout = "_Layout";
}
